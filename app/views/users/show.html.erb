<h2>Profile</h2>
<%= render partial: "profile", locals: {user: @user} %>
<div>
  <% @user.tweets.each do |answer| %>
    <%= render partial: "shared/tweet_block", locals: {tweet: answer} %>
  <% end %>
</div>
